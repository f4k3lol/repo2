platform: ios
username: cisco
password: cisco
data:
  snmp_community: secret
connection_options:
  netconf:
    extras:
      hostkey_verify: False
  scrapli:
    platform: cisco_ios
    port: 22
    extras:
      transport: ssh2
      ssh_config_file: True
      auth_strict_key: False